<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elkha.zik.sakuguru">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sakuguru"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreen" />
        <activity android:name=".JadwalUtama" />
        <activity android:name=".SignUp" />
        <activity android:name=".AbsensiPelajaran" />
        <activity android:name=".AddJadwalActivity" android:label="Add Jadwal" />
        <activity android:name=".ProfileActivity" android:label="Profile"></activity>
    </application>

</manifest>